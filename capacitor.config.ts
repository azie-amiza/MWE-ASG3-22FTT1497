import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ASG3-22FTT1497-MWE',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
